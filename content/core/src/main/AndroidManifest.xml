<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <provider
            android:authorities="${applicationId}.DATABASE_TASK"
            android:name=".BigBrotherDatabaseTask"
            android:enabled="true"
            android:exported="false"/>

        <provider
            android:authorities="${applicationId}.VORTEX"
            android:name=".BigBrotherWatchTask"
            android:enabled="true"
            android:exported="false"/>
    </application>
</manifest>