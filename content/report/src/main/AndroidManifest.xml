<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <activity android:name=".crash.CrashActivity"
            android:theme="@style/Theme.BigBrother"/>

        <provider
            android:authorities="${applicationId}.MINITRUE"
            android:name=".ReportTask"
            android:exported="false"
            android:enabled="true"/>

        <provider
            android:authorities="${applicationId}.MINIPAX"
            android:name=".crash.CrashTask"
            android:enabled="true"
            android:exported="false"/>

    </application>
</manifest>